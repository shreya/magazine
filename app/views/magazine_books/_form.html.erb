<%= form_for(@magazine_book) do |f| %>
  <% if @magazine_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@magazine_book.errors.count, "error") %> prohibited this magazine_book from being saved:</h2>

      <ul>
      <% @magazine_book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
